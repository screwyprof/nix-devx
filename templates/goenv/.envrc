# Claude oauth token can be generated by running `claude setup-token`
if has sops; then
   export CLAUDE_CODE_OAUTH_TOKEN=$(sops -d --extract '["anthropic"]["oath2_token"]' ~/nix-config/secrets/secrets.yaml)
fi

#use flake .

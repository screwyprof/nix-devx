# Minimal Template

A bare-bones flake-parts setup with nixpkgs and a basic devShell.

## Usage

```bash
nix flake init -t github:USER/gopher-env#minimal
```

## What's Included

- flake-parts framework
- nixpkgs (unstable)
- Basic devShell
